rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /session/{sessionId} {
      allow read: if true;
      allow list: if false;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.creator;
      allow update, delete: if request.auth != null && request.auth.uid == resource.data.creator;
    }

    match /session/{sessionId}/event/{eventId} {
      allow read: if true;
      allow list: if false;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.creator;
      allow update, delete: if request.auth != null && request.auth.uid == resource.data.creator;
    }

    // User private data - only the user can access their own
    match /users/{userId} {
      allow read: if true;
      allow list: if false;
      allow write: if request.auth != null && request.auth.uid == userId;

      match /private/user {
        allow list: if false;
        allow read, write:if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}